import DiscordJS, { User, MessageEmbed, Message } from 'discord.js'; 
import config from '../config/config.json'; 
const OCGuide
{
    const OcEmbed = new MessageEmbed()
        .setColor('')
        .setAuthor('Hackintosh Helper')
        .setTitle('Hackintosh Helper OCGuide')
        .setDescription('OpenCore Guide from Dortania. \n Check it out: https://dortania.github.io/OpenCore-Install-Guide/')
    .setFooter("By Hackintosh Helper")
    Message.channel.send(OcEmbed); 
}
export { OCGuide }